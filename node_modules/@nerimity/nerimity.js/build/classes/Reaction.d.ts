import { Channel } from "./Channel";
import { Client } from "./Client";
import { Message } from "./Message";
export declare class Reaction {
    client: Client;
    name: string;
    count?: number;
    emojiId?: string;
    gif: boolean;
    partial: boolean;
    messageId: string;
    message?: Message;
    channel: Channel;
    constructor(client: Client, reaction: {
        messageId: string;
        gif?: boolean;
        name: string;
        emojiId?: string | null;
        channelId: string;
        count?: number;
    });
}
//# sourceMappingURL=Reaction.d.ts.map