"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const Client_1 = require("./classes/Client");
const client = new Client_1.Client();
client.on(Client_1.Events.Ready, () => {
    console.log(`Connected as ${client.user?.username}!`);
});
client.on(Client_1.Events.MessageCreate, (message) => {
    console.log(message.content);
    if (message.content === "!ping") {
        message.reply("Pong!", { buttons: [{ id: "hello", label: "Hello!" }] });
    }
});
client.on(Client_1.Events.MessageButtonClick, (button) => {
    if (button.id === "hello") {
        button.respond({
            title: "Hey!",
            content: `Hey there **${button.user?.username}**!`,
        });
    }
});
client.login("token");
//# sourceMappingURL=example.js.map