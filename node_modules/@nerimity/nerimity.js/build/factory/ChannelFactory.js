"use strict";
// Import the base class and the subclass for runtime construction
Object.defineProperty(exports, "__esModule", { value: true });
exports.createChannel = createChannel;
const Channel_1 = require("../classes/Channel");
const ServerChannel_1 = require("../classes/ServerChannel");
// Define the function that decides which class to instantiate
function createChannel(client, rawChannel) {
    const channelData = rawChannel; // Cast once for convenience
    if (channelData.serverId) {
        // Here, ServerChannel is imported as a runtime value and is available
        return new ServerChannel_1.ServerChannel(client, channelData);
    }
    else {
        return new Channel_1.Channel(client, channelData);
    }
}
//# sourceMappingURL=ChannelFactory.js.map